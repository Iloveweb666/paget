/**
 * 内部常量
 * Internal constants
 */

/** 最大重试次数 / Maximum retry count */
export const LLM_MAX_RETRIES = 2

/** 默认温度参数（较高的随机性有助于自动恢复） / Default temperature (higher randomness helps auto-recovery) */
export const DEFAULT_TEMPERATURE = 0.7
