{
  "name": "paget",
  "version": "0.1.0",
  "private": true,
  "description": "Paget - AI-powered page agent with reflection-before-action",
  "scripts": {
    "predev": "pnpm run build:deps && pnpm run build:bookmarklet",
    "dev": "concurrently -k -n shared,pc,server,ui -c blue,cyan,green,magenta \"pnpm --filter @paget/shared dev\" \"pnpm --filter @paget/page-controller dev\" \"pnpm --filter @paget/server dev\" \"pnpm --filter @paget/ui dev\"",
    "dev:ui": "pnpm --filter @paget/ui dev",
    "dev:server": "pnpm --filter @paget/server dev",
    "build:deps": "pnpm --filter @paget/shared build && pnpm --filter @paget/page-controller build",
    "build": "pnpm run build:deps && concurrently \"pnpm --filter @paget/server build\" \"pnpm --filter @paget/ui build\"",
    "build:shared": "pnpm --filter @paget/shared build",
    "build:page-controller": "pnpm --filter @paget/page-controller build",
    "build:ui": "pnpm --filter @paget/ui build",
    "build:bookmarklet": "pnpm --filter @paget/ui build:bookmarklet",
    "build:server": "pnpm --filter @paget/server build",
    "lint": "eslint packages --ext .ts,.vue",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
